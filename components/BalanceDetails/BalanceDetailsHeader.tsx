import React from "react";
import { Text, StyleSheet, View } from "react-native";

export const BalanceDetailsHeader = ({ title }) => {
  return (
    <View>
      <Text></Text>
    </View>
  );
};

const styles = StyleSheet.create({});
